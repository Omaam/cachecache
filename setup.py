from setuptools import setup

setup(
    name='cachecache',
    version='0.0.1',
    description="Save objet cache and return object cache.",
    long_description="",
    author='Tomoki Omama',
    classifiers=[
        "Development Status :: 1 - Planning"
    ],
    packages=['cachecache']
)
